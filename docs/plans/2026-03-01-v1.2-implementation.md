# V1.2 Implementation Plan — DHCP fixes + Rename/Move TreeView

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Corriger deux bugs DHCP et ajouter renommer/déplacer dans l'arborescence des profils.

**Architecture:** 3 méthodes ajoutées dans `core/profiles.py` (rename, rename_folder, move), 1 fix dans `core/network.py` (apply_dhcp idempotent), 1 fix + enrichissement du menu clic droit dans `modules/tab_config.py`. TDD pour le code core, pas de test UI.

**Tech Stack:** Python 3.14, ttkbootstrap 1.20.1, pytest, os/shutil (stdlib)

---

### Task 1 : Fix `apply_dhcp` — idempotent sur DHCP déjà actif

**Files:**
- Modify: `core/network.py:81-86`
- Test: `tests/test_network.py`

**Step 1 : Écrire le test qui échoue**

Dans `tests/test_network.py`, ajouter après `test_apply_dhcp_calls_netsh` :

```python
@patch("core.network.subprocess.run")
def test_apply_dhcp_already_active_does_not_raise(mock_run):
    """returncode=1 (already DHCP) must not raise."""
    mock_run.return_value = MagicMock(returncode=1)
    apply_dhcp("Ethernet")   # must NOT raise
```

**Step 2 : Vérifier que le test échoue**

```
pytest tests/test_network.py::test_apply_dhcp_already_active_does_not_raise -v
```
Attendu : FAIL — `CalledProcessError` ou similaire.

**Step 3 : Implémenter le fix**

Dans `core/network.py`, remplacer `apply_dhcp` :

```python
def apply_dhcp(iface_name: str) -> None:
    """Passe l'interface en DHCP. Requiert droits admin.
    Idempotent : si l'interface est déjà en DHCP, netsh retourne
    exit code 1 ('pas d'opération') — ce n'est pas une erreur.
    """
    cmd1 = f'netsh interface ip set address name="{iface_name}" source=dhcp'
    subprocess.run(cmd1, capture_output=True, shell=True)  # check=False intentionnel
    cmd2 = f'netsh interface ip set dns name="{iface_name}" source=dhcp'
    subprocess.run(cmd2, capture_output=True, shell=True)  # check=False intentionnel
```

**Step 4 : Vérifier que tous les tests network passent**

```
pytest tests/test_network.py -v
```
Attendu : 5 passed.

**Step 5 : Commit**

```bash
git add core/network.py tests/test_network.py
git commit -m "fix: apply_dhcp is idempotent (ignore exit code 1 = already DHCP)"
```

---

### Task 2 : `ProfileManager.rename()` et `rename_folder()`

**Files:**
- Modify: `core/profiles.py`
- Test: `tests/test_profiles.py`

**Step 1 : Écrire les 2 tests qui échouent**

Dans `tests/test_profiles.py`, ajouter à la fin :

```python
# --- rename tests ---

def test_rename_profile(tmp_path):
    pm = ProfileManager(str(tmp_path))
    pm.save("ancien", SAMPLE)
    pm.rename("ancien", "nouveau")
    assert pm.load("nouveau") == SAMPLE
    assert not (tmp_path / "ancien.json").exists()

def test_rename_profile_in_folder(tmp_path):
    pm = ProfileManager(str(tmp_path))
    pm.save("ancien", SAMPLE, folder="Site")
    pm.rename("ancien", "nouveau", folder="Site")
    assert pm.load("nouveau", folder="Site") == SAMPLE
    assert not (tmp_path / "Site" / "ancien.json").exists()

def test_rename_folder(tmp_path):
    pm = ProfileManager(str(tmp_path))
    pm.create_folder("AncienDossier")
    pm.save("p", SAMPLE, folder="AncienDossier")
    pm.rename_folder("AncienDossier", "NouveauDossier")
    assert "NouveauDossier" in pm.list_tree()
    assert "AncienDossier" not in pm.list_tree()
    assert pm.load("p", folder="NouveauDossier") == SAMPLE
```

**Step 2 : Vérifier que les 3 tests échouent**

```
pytest tests/test_profiles.py::test_rename_profile tests/test_profiles.py::test_rename_profile_in_folder tests/test_profiles.py::test_rename_folder -v
```
Attendu : 3 FAILED — `AttributeError: 'ProfileManager' object has no attribute 'rename'`.

**Step 3 : Implémenter les méthodes**

Dans `core/profiles.py`, ajouter après `delete_folder` :

```python
def rename(self, old_name: str, new_name: str, folder: str = "") -> None:
    """Renomme un profil JSON. Lève FileExistsError si new_name existe déjà."""
    src = self._path(old_name, folder)
    dst = self._path(new_name, folder)
    if os.path.exists(dst):
        raise FileExistsError(f"Un profil '{new_name}' existe déjà.")
    os.rename(src, dst)

def rename_folder(self, old_name: str, new_name: str) -> None:
    """Renomme un dossier de profils. Lève FileExistsError si new_name existe déjà."""
    src = os.path.join(self.profiles_dir, old_name)
    dst = os.path.join(self.profiles_dir, new_name)
    if os.path.exists(dst):
        raise FileExistsError(f"Un dossier '{new_name}' existe déjà.")
    os.rename(src, dst)
```

**Step 4 : Vérifier que tous les tests profiles passent**

```
pytest tests/test_profiles.py -v
```
Attendu : 12 passed.

**Step 5 : Commit**

```bash
git add core/profiles.py tests/test_profiles.py
git commit -m "feat: add ProfileManager.rename() and rename_folder()"
```

---

### Task 3 : `ProfileManager.move()`

**Files:**
- Modify: `core/profiles.py`
- Test: `tests/test_profiles.py`

**Step 1 : Écrire les 2 tests qui échouent**

Dans `tests/test_profiles.py`, ajouter à la fin :

```python
# --- move tests ---

def test_move_profile_to_folder(tmp_path):
    pm = ProfileManager(str(tmp_path))
    pm.save("prof", SAMPLE)
    pm.create_folder("Clients")
    pm.move("prof", from_folder="", to_folder="Clients")
    assert pm.load("prof", folder="Clients") == SAMPLE
    assert not (tmp_path / "prof.json").exists()

def test_move_profile_to_root(tmp_path):
    pm = ProfileManager(str(tmp_path))
    pm.save("prof", SAMPLE, folder="Clients")
    pm.move("prof", from_folder="Clients", to_folder="")
    assert pm.load("prof") == SAMPLE
    assert not (tmp_path / "Clients" / "prof.json").exists()
```

**Step 2 : Vérifier que les 2 tests échouent**

```
pytest tests/test_profiles.py::test_move_profile_to_folder tests/test_profiles.py::test_move_profile_to_root -v
```
Attendu : 2 FAILED — `AttributeError: 'ProfileManager' object has no attribute 'move'`.

**Step 3 : Implémenter la méthode**

Dans `core/profiles.py`, ajouter après `rename_folder` :

```python
def move(self, name: str, from_folder: str, to_folder: str) -> None:
    """Déplace un profil JSON entre dossiers (from_folder="" ou to_folder="" = racine).
    Lève FileExistsError si la cible existe déjà."""
    src = self._path(name, from_folder)
    dst = self._path(name, to_folder)
    if os.path.exists(dst):
        raise FileExistsError(f"Un profil '{name}' existe déjà dans la destination.")
    os.makedirs(os.path.dirname(dst), exist_ok=True)
    shutil.move(src, dst)
```

**Step 4 : Vérifier que tous les tests profiles passent**

```
pytest tests/test_profiles.py -v
```
Attendu : 14 passed.

**Step 5 : Commit**

```bash
git add core/profiles.py tests/test_profiles.py
git commit -m "feat: add ProfileManager.move() between folders"
```

---

### Task 4 : Fix save/load du mode DHCP/Manuel dans tab_config

**Files:**
- Modify: `modules/tab_config.py:115-147`

> Pas de test automatisé (code UI). Vérification manuelle : lancer `python main.py`.

**Step 1 : Modifier `_save_profile`**

Localiser le dict `data` dans `_save_profile` (ligne ~125) et ajouter `"mode"` :

```python
data = {
    "mode": self.mode_var.get(),          # "dhcp" ou "static" — NOUVEAU
    "ip": self.ip_vars["adresseip"].get(),
    "mask": self.ip_vars["masque"].get(),
    "gateway": self.ip_vars["passerelle"].get(),
    "dns1": self.ip_vars["dns1"].get(),
    "dns2": self.ip_vars["dns2"].get(),
}
```

**Step 2 : Modifier `_load_profile`**

Remplacer le contenu de `_load_profile` :

```python
def _load_profile(self, name: str, folder: str = ""):
    try:
        data = self.pm.load(name, folder)
        mode = data.get("mode", "static")   # rétrocompat : défaut static
        self.mode_var.set(mode)
        self._toggle_mode()
        self.ip_vars["adresseip"].set(data.get("ip", ""))
        self.ip_vars["masque"].set(data.get("mask", ""))
        self.ip_vars["passerelle"].set(data.get("gateway", ""))
        self.ip_vars["dns1"].set(data.get("dns1", ""))
        self.ip_vars["dns2"].set(data.get("dns2", ""))
    except FileNotFoundError as e:
        Messagebox.show_error(str(e), title="Erreur")
```

**Step 3 : Vérifier que les tests passent toujours**

```
pytest tests/ -v
```
Attendu : 14 passed (tous).

**Step 4 : Commit**

```bash
git add modules/tab_config.py
git commit -m "fix: save and restore DHCP/Manuel mode in profile JSON"
```

---

### Task 5 : Menu clic droit — Renommer et Déplacer

**Files:**
- Modify: `modules/tab_config.py`

> Code UI uniquement. Pas de test automatisé.

**Step 1 : Ajouter les méthodes privées dans `TabConfig`**

Ajouter après `_delete_folder` (ligne ~159) :

```python
def _rename_profile_node(self, name: str, folder: str = ""):
    new_name = Querybox.get_string(
        "Nouveau nom :", title="Renommer le profil",
        initialvalue=name
    )
    if not new_name or not self._validate_name(new_name.strip()):
        return
    new_name = new_name.strip()
    try:
        self.pm.rename(name, new_name, folder)
        self._refresh_tree()
    except FileExistsError as e:
        Messagebox.show_warning(str(e), title="Nom déjà utilisé")

def _rename_folder_node(self, folder: str):
    new_name = Querybox.get_string(
        "Nouveau nom :", title="Renommer le dossier",
        initialvalue=folder
    )
    if not new_name or not self._validate_name(new_name.strip()):
        return
    new_name = new_name.strip()
    try:
        self.pm.rename_folder(folder, new_name)
        self._refresh_tree()
    except FileExistsError as e:
        Messagebox.show_warning(str(e), title="Nom déjà utilisé")

def _move_profile_node(self, name: str, from_folder: str, to_folder: str):
    try:
        self.pm.move(name, from_folder, to_folder)
        self._refresh_tree()
    except FileExistsError as e:
        Messagebox.show_warning(str(e), title="Conflit de nom")
```

**Step 2 : Mettre à jour `_on_tree_right_click`**

Remplacer la méthode entière :

```python
def _on_tree_right_click(self, event):
    iid = self.tree.identify_row(event.y)
    if iid:
        self.tree.focus(iid)
    menu = tk.Menu(self, tearoff=0)

    if iid and iid.startswith("folder::"):
        folder = iid[len("folder::"):]
        menu.add_command(label="Renommer le dossier",
                         command=lambda: self._rename_folder_node(folder))
        menu.add_separator()
        menu.add_command(label="Supprimer le dossier",
                         command=lambda: self._delete_folder(folder))

    elif iid and iid.startswith("profile::"):
        parts = iid.split("::")
        if len(parts) < 3:
            return
        folder, name = parts[1], parts[2]

        # ---- Renommer ----
        menu.add_command(label="Renommer le profil",
                         command=lambda: self._rename_profile_node(name, folder))

        # ---- Déplacer vers ▸ ----
        tree_data = self.pm.list_tree()
        # destinations = tous les dossiers sauf celui d'origine
        destinations = []
        if folder != "":                          # actuellement dans un dossier → proposer racine
            destinations.append(("Racine", ""))
        for f in sorted(k for k in tree_data if k and k != folder):
            destinations.append((f, f))

        if destinations:
            move_menu = tk.Menu(menu, tearoff=0)
            for label, target_folder in destinations:
                move_menu.add_command(
                    label=label,
                    command=lambda n=name, src=folder, dst=target_folder:
                        self._move_profile_node(n, src, dst)
                )
            menu.add_cascade(label="Déplacer vers \u25b8", menu=move_menu)

        menu.add_separator()
        menu.add_command(label="Supprimer le profil",
                         command=lambda: self._delete_profile_node(name, folder))

    else:
        menu.add_command(label="Nouveau dossier", command=self._new_folder)

    try:
        menu.tk_popup(event.x_root, event.y_root)
    finally:
        menu.grab_release()
```

**Step 3 : Vérifier que les tests passent toujours**

```
pytest tests/ -v
```
Attendu : 14 passed.

**Step 4 : Vérification manuelle**

```
python main.py
```
Tester :
- Clic droit sur profil → "Renommer le profil" → saisir nouveau nom → vérifier dans l'arbre
- Clic droit sur dossier → "Renommer le dossier" → vérifier dans l'arbre
- Clic droit sur profil racine → "Déplacer vers ▸ [dossier]" → vérifier déplacement
- Clic droit sur profil dans dossier → "Déplacer vers ▸ Racine" → vérifier

**Step 5 : Commit**

```bash
git add modules/tab_config.py
git commit -m "feat: add rename and move to TreeView right-click menu"
```

---

### Task 6 : Push + fin de branche

**Step 1 : Lancer la suite complète**

```
pytest tests/ -v
```
Attendu : 14 passed.

**Step 2 : Push**

```bash
git push origin develop
```

**Step 3 : Invoquer finishing-a-development-branch**

Utiliser le skill `superpowers:finishing-a-development-branch` pour présenter les options de merge/PR.
