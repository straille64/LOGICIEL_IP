# V2.1 Modbus UX — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Améliorer l'UX de l'onglet Modbus avec cache/rafraîchissement dynamique,
modes "Octet" et "CHAMP DE BITS", et une popup de détails au double-clic.

**Architecture:**
- Task A (1-3) : modifications de `core/modbus.py` et `modules/tab_modbus.py`
  — cache, constantes, bindings, modes spéciaux.
- Task B (4-5) : nouveau fichier `modules/dialog_register_detail.py` + wiring double-clic.
- Séparation stricte : les calculs purs sont testés unitairement sans tkinter.

**Tech Stack:** Python 3.14, ttkbootstrap 1.20.1, tkinter, pytest

---

## Task 1 : core/modbus.py — Remplacer "ascii" → "octet"

**Files:**
- Modify: `core/modbus.py` (case "ascii" → case "octet")
- Modify: `tests/test_modbus.py` (mettre à jour les tests)

---

**Step 1 : Mettre à jour le test existant et ajouter 2 nouveaux**

Dans `tests/test_modbus.py`, remplacer `test_format_ascii` par `test_format_octet`
et ajouter `test_format_octet_high_byte` :

```python
def test_format_octet():
    # Registre 22 = 0x0016 → octet haut=0, octet bas=22
    assert format_register_value(22, "octet") == "0 | 22"

def test_format_octet_high_byte():
    # Registre 0x1600 = 5632 → octet haut=22, octet bas=0
    assert format_register_value(0x1600, "octet") == "22 | 0"

def test_format_octet_max():
    # Registre 0xFFFF → 255 | 255
    assert format_register_value(0xFFFF, "octet") == "255 | 255"
```

**Step 2 : Vérifier que les 3 tests échouent**

```bash
C:\Python314\python.exe -m pytest tests/test_modbus.py::test_format_octet tests/test_modbus.py::test_format_octet_high_byte tests/test_modbus.py::test_format_octet_max -v
```
Attendu : 3 FAILED (case "ascii" → retourne des caractères, pas "0 | 22")

**Step 3 : Modifier `core/modbus.py` — remplacer le case "ascii"**

Chercher le bloc :
```python
        case "ascii":
            high = (r0 >> 8) & 0xFF
            low = r0 & 0xFF
            return "".join(chr(b) if 32 <= b < 127 else "." for b in [high, low])
```

Remplacer par :
```python
        case "octet":
            high = (r0 >> 8) & 0xFF
            low  = r0 & 0xFF
            return f"{high} | {low}"
```

**Step 4 : Vérifier que les 3 tests passent + aucune régression**

```bash
C:\Python314\python.exe -m pytest tests/ -v
```
Attendu : 56 passed (dont les 3 nouveaux, et test_format_ascii supprimé)

**Step 5 : Commit**

```bash
git add core/modbus.py tests/test_modbus.py
git commit -m "feat: replace ascii display mode with octet (HH | LL) in format_register_value"
```

---

## Task 2 : tab_modbus.py — Constantes + mode "CHAMP DE BITS"

**Files:**
- Modify: `modules/tab_modbus.py` (constantes DISPLAY_MODES, logique _populate_table)

---

**Step 1 : Mettre à jour DISPLAY_MODES (lignes 29-38)**

Remplacer le bloc complet :
```python
DISPLAY_MODES = [
    ("Mot 16 bits",            "uint16"),
    ("Signé 16 bits",          "int16"),
    ("Float 32 bits (2 reg)",  "float32"),
    ("Entier 32 bits (2 reg)", "uint32"),
    ("Signé 32 bits (2 reg)",  "int32"),
    ("Binaire",                "bin"),
    ("ASCII",                  "ascii"),
]
DISPLAY_LABELS = [d[0] for d in DISPLAY_MODES]
DISPLAY_KEY    = {d[0]: d[1] for d in DISPLAY_MODES}
```

Par :
```python
DISPLAY_MODES = [
    ("Mot 16 bits",            "uint16"),
    ("Signé 16 bits",          "int16"),
    ("Float 32 bits (2 reg)",  "float32"),
    ("Entier 32 bits (2 reg)", "uint32"),
    ("Signé 32 bits (2 reg)",  "int32"),
    ("Binaire",                "bin"),
    ("Octet (H|L)",            "octet"),
    ("CHAMP DE BITS",          "bits"),
]
DISPLAY_LABELS = [d[0] for d in DISPLAY_MODES]
DISPLAY_KEY    = {d[0]: d[1] for d in DISPLAY_MODES}
```

**Step 2 : Modifier `_populate_table` pour gérer le mode "bits"**

Remplacer le corps de `_populate_table` (lignes 429-447) par :

```python
def _populate_table(self, base_address: int, values: list):
    self.tree.delete(*self.tree.get_children())
    if not values:
        return
    display_mode = DISPLAY_KEY.get(self.display_var.get(), "uint16")
    num_base     = {"DÉCIMAL": "dec", "HEX": "hex", "BINAIRE": "bin"}.get(
        self.nummode_var.get(), "dec")
    swap_bytes = self.swap_bytes_var.get()
    swap_words = self.swap_words_var.get()
    unsigned   = self.unsigned_var.get()

    # Mode spécial : décomposer chaque registre en 16 bits individuels
    if display_mode == "bits":
        for i, raw_val in enumerate(values):
            reg_addr = base_address + i
            v = int(raw_val)
            for bit in range(16):
                bit_val = (v >> bit) & 1
                self.tree.insert("", END, values=(f"{reg_addr}.{bit:02d}", str(bit_val)))
        return

    step = 2 if display_mode in ("float32", "uint32", "int32") else 1
    i = 0
    while i < len(values):
        reg_addr = base_address + i
        raw = values[i:i + step] if step == 2 else values[i]
        formatted = format_register_value(
            raw, display_mode, num_base, swap_bytes, swap_words, unsigned
        )
        self.tree.insert("", END, values=(reg_addr, formatted))
        i += step
```

**Step 3 : Vérifier l'import et les tests**

```bash
C:\Python314\python.exe -c "from modules.tab_modbus import TabModbus; print('OK')"
C:\Python314\python.exe -m pytest tests/ -v
```
Attendu : Import OK, 56 passed

**Step 4 : Commit**

```bash
git add modules/tab_modbus.py
git commit -m "feat: add Octet and CHAMP DE BITS display modes to tab_modbus"
```

---

## Task 3 : tab_modbus.py — Cache + rafraîchissement dynamique

**Files:**
- Modify: `modules/tab_modbus.py`

---

**Step 1 : Ajouter les attributs de cache dans `__init__`**

Localiser le bloc `__init__` (lignes 55-61) :
```python
    def __init__(self, master, **kwargs):
        super().__init__(master, **kwargs)
        self.mc = ModbusClient()
        self._stop_event = threading.Event()
        self._poll_thread = None
        self._cyclic_interval = 1.0
        self._conn_params: dict = {}
```

Ajouter les 2 attributs de cache à la fin :
```python
        self._raw_cache: list = []
        self._cache_address: int = 0
```

**Step 2 : Modifier `_populate_table` pour sauvegarder dans le cache**

Au début de `_populate_table`, juste après `self.tree.delete(...)` et le `if not values: return`,
ajouter les 2 lignes de sauvegarde :

```python
    def _populate_table(self, base_address: int, values: list):
        self.tree.delete(*self.tree.get_children())
        if not values:
            return
        # Sauvegarder dans le cache pour rafraîchissement dynamique
        self._cache_address = base_address
        self._raw_cache = list(values)
        # ... reste inchangé
```

**Step 3 : Ajouter la méthode `_refresh_display`**

Ajouter cette méthode juste avant `_build_connection_bar` (ou après `_populate_table`) :

```python
    def _refresh_display(self, *_):
        """Recalcule l'affichage depuis le cache sans nouvelle requête Modbus."""
        if self._raw_cache:
            self._populate_table(self._cache_address, self._raw_cache)
```

**Step 4 : Vider le cache dans `_do_disconnect`**

Localiser `_do_disconnect` :
```python
    def _do_disconnect(self):
        self._stop_cyclic()
        self.mc.disconnect()
        self._set_status("Déconnecté", connected=False)
```

Ajouter la réinitialisation du cache :
```python
    def _do_disconnect(self):
        self._stop_cyclic()
        self.mc.disconnect()
        self._raw_cache = []
        self._cache_address = 0
        self._set_status("Déconnecté", connected=False)
```

**Step 5 : Ajouter les bindings dans `_build_left_panel`**

Localiser `_build_left_panel`. Après les lignes qui créent `swap_bytes_var`, `swap_words_var`,
`unsigned_var` (lignes 242-244) et la Combobox `display_var` (lignes 255-259),
ajouter les bindings **après la création de la Combobox** :

```python
        # Rafraîchissement dynamique — bindings après création de tous les widgets
        for var in (self.swap_bytes_var, self.swap_words_var, self.unsigned_var):
            var.trace_add("write", self._refresh_display)
        self.display_var.trace_add("write", self._refresh_display)
```

**ATTENTION :** ces 4 lignes doivent être à la fin de `_build_left_panel`, après que
`display_var` soit défini (ligne 255). Vérifier que `self.display_var` est bien défini
avant d'appeler `trace_add`.

**Step 6 : Vérifier l'import et les tests**

```bash
C:\Python314\python.exe -c "from modules.tab_modbus import TabModbus; print('OK')"
C:\Python314\python.exe -m pytest tests/ -v
```
Attendu : Import OK, 56 passed

**Step 7 : Commit**

```bash
git add modules/tab_modbus.py
git commit -m "feat: add raw data cache and dynamic display refresh to tab_modbus"
```

---

## Task 4 : modules/dialog_register_detail.py — Popup de détails

**Files:**
- Create: `modules/dialog_register_detail.py`
- Create: `tests/test_dialog_detail.py`

---

**Step 1 : Écrire les tests unitaires (fonctions pures, sans tkinter)**

Créer `tests/test_dialog_detail.py` :

```python
"""tests/test_dialog_detail.py — Tests unitaires des calculs de RegisterDetailDialog."""
import struct
import pytest
from modules.dialog_register_detail import decode_registers, fmt_bin_byte, fmt_ascii_byte, fmt_reel32


# ─── decode_registers ─────────────────────────────────────────────────────────

def test_decode_no_swap():
    # w0=0x1234, w1=0x5678 → combined=0x12345678
    # b3=0x12=18, b2=0x34=52, b1=0x56=86, b0=0x78=120
    assert decode_registers(0x1234, 0x5678) == (0x12, 0x34, 0x56, 0x78)

def test_decode_swap_words():
    # swap_words : hi=w1, lo=w0 → combined=0x56781234
    assert decode_registers(0x1234, 0x5678, swap_words=True) == (0x56, 0x78, 0x12, 0x34)

def test_decode_swap_bytes():
    # swap_bytes : échange b3↔b2 et b1↔b0
    # sans swap: (0x12, 0x34, 0x56, 0x78) → avec: (0x34, 0x12, 0x78, 0x56)
    assert decode_registers(0x1234, 0x5678, swap_bytes=True) == (0x34, 0x12, 0x78, 0x56)

def test_decode_both_swaps():
    b3, b2, b1, b0 = decode_registers(0x1234, 0x5678, swap_words=True, swap_bytes=True)
    # swap_words → (0x56, 0x78, 0x12, 0x34), swap_bytes → (0x78, 0x56, 0x34, 0x12)
    assert (b3, b2, b1, b0) == (0x78, 0x56, 0x34, 0x12)


# ─── Helpers de formatage ─────────────────────────────────────────────────────

def test_fmt_bin_byte():
    assert fmt_bin_byte(0b10110010) == "10110010"
    assert fmt_bin_byte(0) == "00000000"
    assert fmt_bin_byte(255) == "11111111"

def test_fmt_ascii_byte_printable():
    assert fmt_ascii_byte(ord("A")) == "A"
    assert fmt_ascii_byte(ord(" ")) == " "

def test_fmt_ascii_byte_non_printable():
    assert fmt_ascii_byte(0) == "."
    assert fmt_ascii_byte(31) == "."
    assert fmt_ascii_byte(127) == "."

def test_fmt_reel32():
    # IEEE 754 : 0x3F800000 = 1.0
    result = fmt_reel32(0x3F80, 0x0000)
    assert result == "1.0"

def test_fmt_reel32_nan():
    # Valeur qui génère NaN → retourne "NaN"
    result = fmt_reel32(0x7FC0, 0x0000)
    assert "N" in result.upper()
```

**Step 2 : Vérifier que les tests échouent (module inexistant)**

```bash
C:\Python314\python.exe -m pytest tests/test_dialog_detail.py -v
```
Attendu : ERROR (ImportError — `dialog_register_detail` n'existe pas encore)

**Step 3 : Créer `modules/dialog_register_detail.py`**

```python
"""modules/dialog_register_detail.py — Popup de détails d'un registre Modbus."""
import struct
import tkinter as tk
import ttkbootstrap as ttk
from ttkbootstrap.constants import *


# ─── Fonctions pures (testables sans tkinter) ─────────────────────────────────

def decode_registers(
    w0: int, w1: int,
    swap_words: bool = False,
    swap_bytes: bool = False,
) -> tuple[int, int, int, int]:
    """Décode 2 registres 16 bits en 4 octets (b3, b2, b1, b0).

    b3 = MSB (Octet 3), b0 = LSB (Octet 0).
    swap_words : inverse l'ordre des deux mots avant décodage.
    swap_bytes : échange b3↔b2 et b1↔b0 après décodage.
    """
    hi, lo = (w1, w0) if swap_words else (w0, w1)
    combined = ((hi & 0xFFFF) << 16) | (lo & 0xFFFF)
    b3 = (combined >> 24) & 0xFF
    b2 = (combined >> 16) & 0xFF
    b1 = (combined >>  8) & 0xFF
    b0 =  combined        & 0xFF
    if swap_bytes:
        b3, b2 = b2, b3
        b1, b0 = b0, b1
    return b3, b2, b1, b0


def fmt_bin_byte(b: int) -> str:
    """Retourne la représentation binaire sur 8 bits d'un octet."""
    return f"{b & 0xFF:08b}"


def fmt_ascii_byte(b: int) -> str:
    """Retourne le caractère ASCII ou '.' si non imprimable."""
    return chr(b) if 32 <= b < 127 else "."


def fmt_reel32(w0: int, w1: int) -> str:
    """Décode deux mots 16 bits en flottant IEEE 754 big-endian."""
    combined = ((w0 & 0xFFFF) << 16) | (w1 & 0xFFFF)
    try:
        val = struct.unpack(">f", combined.to_bytes(4, "big"))[0]
        return str(val)
    except Exception:
        return "NaN"


# ─── Dialogue ─────────────────────────────────────────────────────────────────

class RegisterDetailDialog(tk.Toplevel):
    """Popup de détails d'un registre Modbus (lecture seule).

    Paramètres :
        parent  : widget parent
        address : adresse du registre cliqué
        w0      : valeur du registre (16 bits)
        w1      : valeur du registre suivant (16 bits, 0 si absent)
    """

    def __init__(self, parent, address: int, w0: int, w1: int):
        super().__init__(parent)
        self.title(f"Détails — N° Registre : {address}")
        self.resizable(False, False)
        self.transient(parent)

        self._address = address
        self._w0 = w0 & 0xFFFF
        self._w1 = w1 & 0xFFFF

        # Options locales
        self.hexa_var        = tk.BooleanVar(value=False)
        self.unsigned_var    = tk.BooleanVar(value=False)
        self.swap_bytes_var  = tk.BooleanVar(value=False)
        self.swap_words_var  = tk.BooleanVar(value=False)

        # StringVars pour les cellules du tableau
        self._cell_vars: dict[str, list[tk.StringVar]] = {}
        self._bit_vars: list[tk.BooleanVar] = []  # bits 0-31

        self._build()
        self._refresh()

        # Centrage sur le parent
        self.update_idletasks()
        px = parent.winfo_rootx() + parent.winfo_width() // 2
        py = parent.winfo_rooty() + parent.winfo_height() // 2
        w, h = self.winfo_width(), self.winfo_height()
        self.geometry(f"+{px - w // 2}+{py - h // 2}")

        self.grab_set()
        self.wait_window()

    # ─── Construction UI ──────────────────────────────────────────────────────

    def _build(self):
        outer = ttk.Frame(self, padding=10)
        outer.pack(fill=BOTH, expand=True)

        self._build_bits(outer)
        ttk.Separator(outer, orient=HORIZONTAL).pack(fill=X, pady=6)
        self._build_grid(outer)
        ttk.Separator(outer, orient=HORIZONTAL).pack(fill=X, pady=6)
        self._build_buttons(outer)

    def _build_bits(self, parent):
        """Deux rangées de 16 checkboxes read-only représentant les bits."""
        frame = ttk.LabelFrame(parent, text=f"Bits — N° Registre : {self._address}")
        frame.pack(fill=X)

        self._bit_vars = [tk.BooleanVar(value=False) for _ in range(32)]

        for row_idx, (start, end, label_prefix) in enumerate([
            (15, -1, ""),   # bits 15→0 du mot w0
            (31, 15, ""),   # bits 31→16 du mot w1
        ]):
            # Numéros de bits
            num_row = ttk.Frame(frame)
            num_row.pack()
            bit_row = ttk.Frame(frame)
            bit_row.pack()
            for col, bit in enumerate(range(start, end, -1)):
                ttk.Label(num_row, text=str(bit), width=3, anchor=CENTER).grid(
                    row=0, column=col)
                cb = ttk.Checkbutton(
                    bit_row,
                    variable=self._bit_vars[bit],
                    state=DISABLED,
                    bootstyle="square-toggle" if bit >= 16 else "square-toggle",
                )
                cb.grid(row=0, column=col)

    def _build_grid(self, parent):
        """Grille centrale : 4 colonnes Octet × 6 types de valeurs + Options."""
        container = ttk.Frame(parent)
        container.pack(fill=X)

        # ── Table ──────────────────────────────────────────────────────────
        table = ttk.Frame(container)
        table.grid(row=0, column=0, sticky=W)

        headers = ["", "Octet 3", "Octet 2", "Octet 1", "Octet 0"]
        for c, h in enumerate(headers):
            ttk.Label(table, text=h, width=10, anchor=CENTER,
                      bootstyle=PRIMARY).grid(row=0, column=c, padx=1, pady=1)

        row_names = ["Binaire", "Ascii", "Octet", "Mot 16", "Mot 32", "Réel 32"]
        for r, name in enumerate(row_names, start=1):
            ttk.Label(table, text=name, width=10, anchor=W,
                      bootstyle=SECONDARY).grid(row=r, column=0, padx=1, pady=1)

        # StringVars pour chaque cellule
        for key in ("binaire", "ascii", "octet"):
            self._cell_vars[key] = [tk.StringVar(value="") for _ in range(4)]
            for c, sv in enumerate(self._cell_vars[key], start=1):
                ttk.Label(table, textvariable=sv, width=10, anchor=CENTER,
                          relief="groove").grid(
                    row=list(row_names).index(key.capitalize()) + 1 if key != "ascii" else 2,
                    column=c, padx=1, pady=1)

        # Binaire row (row 1)
        for c, sv in enumerate(self._cell_vars["binaire"], start=1):
            ttk.Label(table, textvariable=sv, width=10, anchor=CENTER,
                      relief="groove").grid(row=1, column=c, padx=1, pady=1)

        # Ascii row (row 2)
        for c, sv in enumerate(self._cell_vars["ascii"], start=1):
            ttk.Label(table, textvariable=sv, width=10, anchor=CENTER,
                      relief="groove").grid(row=2, column=c, padx=1, pady=1)

        # Octet row (row 3)
        for c, sv in enumerate(self._cell_vars["octet"], start=1):
            ttk.Label(table, textvariable=sv, width=10, anchor=CENTER,
                      relief="groove").grid(row=3, column=c, padx=1, pady=1)

        # Mot 16 row (row 4) — 2 cellules colspan 2
        self._mot16_vars = [tk.StringVar(value=""), tk.StringVar(value="")]
        ttk.Label(table, textvariable=self._mot16_vars[0], width=21, anchor=CENTER,
                  relief="groove").grid(row=4, column=1, columnspan=2, padx=1, pady=1)
        ttk.Label(table, textvariable=self._mot16_vars[1], width=21, anchor=CENTER,
                  relief="groove").grid(row=4, column=3, columnspan=2, padx=1, pady=1)

        # Mot 32 row (row 5) — 1 cellule colspan 4
        self._mot32_var = tk.StringVar(value="")
        ttk.Label(table, textvariable=self._mot32_var, width=43, anchor=CENTER,
                  relief="groove").grid(row=5, column=1, columnspan=4, padx=1, pady=1)

        # Réel 32 row (row 6) — 1 cellule colspan 4
        self._reel32_var = tk.StringVar(value="")
        ttk.Label(table, textvariable=self._reel32_var, width=43, anchor=CENTER,
                  relief="groove").grid(row=6, column=1, columnspan=4, padx=1, pady=1)

        # ── Options ────────────────────────────────────────────────────────
        opts = ttk.LabelFrame(container, text="Options", padding=8)
        opts.grid(row=0, column=1, sticky=N+W, padx=(12, 0))

        for text, var in [
            ("Hexa",         self.hexa_var),
            ("Non signé",    self.unsigned_var),
            ("Inv. Octets",  self.swap_bytes_var),
            ("Inv. Mots",    self.swap_words_var),
        ]:
            ttk.Checkbutton(opts, text=text, variable=var,
                            command=self._refresh).pack(anchor=W, pady=2)

    def _build_buttons(self, parent):
        btn_frame = ttk.Frame(parent)
        btn_frame.pack(fill=X)
        ttk.Button(btn_frame, text="OK", bootstyle=PRIMARY,
                   command=self.destroy, width=18).pack(side=LEFT, padx=6)
        ttk.Button(btn_frame, text="Annuler", bootstyle=SECONDARY,
                   command=self.destroy, width=18).pack(side=RIGHT, padx=6)

    # ─── Logique de rafraîchissement ──────────────────────────────────────────

    def _refresh(self):
        """Recalcule toutes les cellules depuis w0/w1 et les options courantes."""
        sw = self.swap_words_var.get()
        sb = self.swap_bytes_var.get()
        hexa = self.hexa_var.get()
        unsigned = self.unsigned_var.get()

        b3, b2, b1, b0 = decode_registers(self._w0, self._w1, sw, sb)
        bytes_list = [b3, b2, b1, b0]

        # Bits checkboxes (w0 et w1, non affectés par swap)
        for i in range(16):
            self._bit_vars[i].set(bool((self._w0 >> i) & 1))
        for i in range(16):
            self._bit_vars[16 + i].set(bool((self._w1 >> i) & 1))

        # Binaire
        for i, sv in enumerate(self._cell_vars["binaire"]):
            sv.set(fmt_bin_byte(bytes_list[i]))

        # Ascii
        for i, sv in enumerate(self._cell_vars["ascii"]):
            sv.set(fmt_ascii_byte(bytes_list[i]))

        # Octet
        def _fmt_int(v: int) -> str:
            return f"0x{v:02X}" if hexa else str(v)

        for i, sv in enumerate(self._cell_vars["octet"]):
            sv.set(_fmt_int(bytes_list[i]))

        # Mot 16 : w0 et w1 (appliqués après swap_words)
        if sw:
            word_hi, word_lo = self._w1, self._w0
        else:
            word_hi, word_lo = self._w0, self._w1

        def _fmt_word16(w: int) -> str:
            if hexa:
                return f"0x{w & 0xFFFF:04X}"
            if not unsigned and (w & 0x8000):
                return str((w & 0xFFFF) - 0x10000)
            return str(w & 0xFFFF)

        self._mot16_vars[0].set(_fmt_word16(word_hi))
        self._mot16_vars[1].set(_fmt_word16(word_lo))

        # Mot 32
        combined = ((word_hi & 0xFFFF) << 16) | (word_lo & 0xFFFF)
        if hexa:
            self._mot32_var.set(f"0x{combined:08X}")
        elif not unsigned and (combined & 0x80000000):
            self._mot32_var.set(str(combined - 0x100000000))
        else:
            self._mot32_var.set(str(combined))

        # Réel 32
        self._reel32_var.set(fmt_reel32(word_hi, word_lo))
```

**Step 4 : Vérifier que les tests passent**

```bash
C:\Python314\python.exe -m pytest tests/test_dialog_detail.py -v
```
Attendu : 9 PASSED

**Step 5 : Vérifier l'ensemble de la suite**

```bash
C:\Python314\python.exe -m pytest tests/ -v
```
Attendu : 65 passed (56 existants + 9 nouveaux)

**Step 6 : Commit**

```bash
git add modules/dialog_register_detail.py tests/test_dialog_detail.py
git commit -m "feat: add RegisterDetailDialog with decode_registers and display grid"
```

---

## Task 5 : tab_modbus.py — Wiring double-clic

**Files:**
- Modify: `modules/tab_modbus.py`

---

**Step 1 : Ajouter l'import en haut de `tab_modbus.py`**

Localiser les imports (lignes 1-12), ajouter après `from core.modbus import ...` :

```python
from modules.dialog_register_detail import RegisterDetailDialog
```

**Step 2 : Ajouter le binding double-clic dans `_build_right_panel`**

Localiser `_build_right_panel` (ligne 261). Après `vsb.pack(side=RIGHT, fill=Y)`,
ajouter le binding sur `self.tree` :

```python
        self.tree.bind("<Double-1>", self._on_tree_double_click)
```

**Step 3 : Ajouter la méthode `_on_tree_double_click`**

Ajouter cette méthode dans la classe, près de `_populate_table` :

```python
    def _on_tree_double_click(self, event):
        """Ouvre la popup de détails pour le registre double-cliqué."""
        item = self.tree.identify_row(event.y)
        if not item:
            return
        reg_str = self.tree.set(item, "reg")
        # Gérer le format "100.00" (mode CHAMP DE BITS) → prendre juste l'adresse entière
        try:
            address = int(str(reg_str).split(".")[0])
        except (ValueError, AttributeError):
            return
        if not self._raw_cache:
            return
        i = address - self._cache_address
        if not (0 <= i < len(self._raw_cache)):
            return
        w0 = int(self._raw_cache[i]) if not isinstance(self._raw_cache[i], bool) else int(self._raw_cache[i])
        w1 = int(self._raw_cache[i + 1]) if (i + 1) < len(self._raw_cache) else 0
        RegisterDetailDialog(self, address, w0, w1)
```

**Step 4 : Vérifier l'import et les tests**

```bash
C:\Python314\python.exe -c "from modules.tab_modbus import TabModbus; print('OK')"
C:\Python314\python.exe -m pytest tests/ -v
```
Attendu : Import OK, 65 passed

**Step 5 : Commit**

```bash
git add modules/tab_modbus.py
git commit -m "feat: wire double-click on register tree to RegisterDetailDialog"
```

---

## Vérification finale

```bash
C:\Python314\python.exe -m pytest tests/ -v
```
Attendu : **65 passed**

```bash
C:\Python314\python.exe -m PyInstaller build.spec --clean
```
Attendu : `Build complete!` → `dist/LOGICIEL_IP.exe`

```bash
git log --oneline -6
```
