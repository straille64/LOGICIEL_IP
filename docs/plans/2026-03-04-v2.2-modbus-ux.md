# V2.2 Modbus UX — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Ajouter un quadrillage au tableau principal et refaire la popup de détails avec tous les champs éditables et synchronisés en temps réel (Approche C — tout éditable, bits cliquables).

**Architecture:**
- Task 1 : quadrillage Treeview dans `tab_modbus.py` (colonnes "sep" + alternance de couleurs).
- Tasks 2-4 : refonte complète de `dialog_register_detail.py` — nouvelles fonctions pures de parsing (TDD), Entry à la place des Labels, bits cliquables, synchronisation anti-boucle via flag `_updating`.

**Tech Stack:** Python 3.14, ttkbootstrap 1.20.1, tkinter, pytest

---

## Task 1 : tab_modbus.py — Quadrillage Treeview

**Files:**
- Modify: `modules/tab_modbus.py`

---

**Step 1 : Modifier `_build_right_panel` — ajouter la colonne séparateur et les tags**

Dans `_build_right_panel` (ligne 270), remplacer :
```python
        self.tree = ttk.Treeview(
            table_frame,
            columns=("reg", "val"),
            show="headings",
            selectmode="browse",
        )
        self.tree.heading("reg", text="N° Registre")
        self.tree.heading("val", text="Valeur")
        self.tree.column("reg", width=130, anchor=CENTER)
        self.tree.column("val", width=200, anchor=W)
```

Par :
```python
        self.tree = ttk.Treeview(
            table_frame,
            columns=("reg", "sep", "val"),
            show="headings",
            selectmode="browse",
        )
        self.tree.heading("reg", text="N° Registre")
        self.tree.heading("sep", text="")
        self.tree.heading("val", text="Valeur")
        self.tree.column("reg",  width=130, anchor=CENTER)
        self.tree.column("sep",  width=2, minwidth=2, stretch=False)
        self.tree.column("val",  width=200, anchor=W)

        # Quadrillage : alternance de couleurs par ligne
        self.tree.tag_configure("oddrow",  background="#2b3035")
        self.tree.tag_configure("evenrow", background="#1e2124")
```

---

**Step 2 : Modifier `_populate_table` — injecter la colonne sep et les tags**

Dans `_populate_table` (ligne 441), modifier les deux `self.tree.insert(...)` :

**Mode "bits" (ligne 462)** :
```python
                    self.tree.insert("", END, values=(f"{reg_addr}.{bit:02d}", "", str(bit_val)))
```

**Boucle principale (ligne 473)** :
Ajouter un compteur `row_idx` et passer les tags :
```python
        step = 2 if display_mode in ("float32", "uint32", "int32") else 1
        i = 0
        row_idx = 0
        while i < len(values):
            reg_addr = base_address + i
            raw = values[i:i + step] if step == 2 else values[i]
            formatted = format_register_value(
                raw, display_mode, num_base, swap_bytes, swap_words, unsigned
            )
            tag = "oddrow" if row_idx % 2 else "evenrow"
            self.tree.insert("", END, values=(reg_addr, "", formatted), tags=(tag,))
            i += step
            row_idx += 1
```

Faire de même pour le mode "bits" (ajouter `row_idx` dans la boucle bits) :
```python
        if display_mode == "bits":
            row_idx = 0
            for i, raw_val in enumerate(values):
                reg_addr = base_address + i
                v = int(raw_val)
                for bit in range(16):
                    bit_val = (v >> bit) & 1
                    tag = "oddrow" if row_idx % 2 else "evenrow"
                    self.tree.insert("", END, values=(f"{reg_addr}.{bit:02d}", "", str(bit_val)), tags=(tag,))
                    row_idx += 1
            return
```

---

**Step 3 : Vérifier l'import et les tests**

```bash
C:\Python314\python.exe -c "from modules.tab_modbus import TabModbus; print('OK')"
C:\Python314\python.exe -m pytest tests/ -v
```
Attendu : Import OK, 67 passed

---

**Step 4 : Commit**

```bash
git add modules/tab_modbus.py
git commit -m "feat: add gridlines (alternating rows + separator column) to Modbus Treeview"
```

---

## Task 2 : dialog_register_detail.py — Fonctions pures de parsing (TDD)

**Files:**
- Modify: `modules/dialog_register_detail.py` (ajouter 5 fonctions pures)
- Modify: `tests/test_dialog_detail.py` (ajouter 10 tests)

---

**Step 1 : Écrire les tests (TDD — avant le code)**

Ajouter à la fin de `tests/test_dialog_detail.py` :

```python
from modules.dialog_register_detail import (
    parse_bin_byte, parse_octet, parse_mot16, parse_mot32, parse_reel32_to_words
)

# ─── parse_bin_byte ───────────────────────────────────────────────────────────

def test_parse_bin_byte_valid():
    assert parse_bin_byte("10110010") == 178

def test_parse_bin_byte_zero():
    assert parse_bin_byte("00000000") == 0

def test_parse_bin_byte_invalid():
    import pytest
    with pytest.raises(ValueError):
        parse_bin_byte("xyz")

# ─── parse_octet ──────────────────────────────────────────────────────────────

def test_parse_octet_decimal():
    assert parse_octet("255") == 255

def test_parse_octet_hex():
    assert parse_octet("0xFF") == 255

def test_parse_octet_out_of_range():
    import pytest
    with pytest.raises(ValueError):
        parse_octet("300")

# ─── parse_mot16 ──────────────────────────────────────────────────────────────

def test_parse_mot16():
    assert parse_mot16("0x1234") == 0x1234

def test_parse_mot16_negative_raises():
    import pytest
    with pytest.raises(ValueError):
        parse_mot16("-1")

# ─── parse_mot32 ──────────────────────────────────────────────────────────────

def test_parse_mot32_split():
    val = parse_mot32("0x12345678")
    assert (val >> 16) == 0x1234
    assert (val & 0xFFFF) == 0x5678

# ─── parse_reel32_to_words ────────────────────────────────────────────────────

def test_parse_reel32_to_words():
    # 1.0 = 0x3F800000 → w0=0x3F80, w1=0x0000
    w0, w1 = parse_reel32_to_words("1.0")
    assert w0 == 0x3F80
    assert w1 == 0x0000
```

---

**Step 2 : Vérifier que les 10 tests échouent (ImportError)**

```bash
C:\Python314\python.exe -m pytest tests/test_dialog_detail.py -v
```
Attendu : ERROR (ImportError — fonctions pas encore définies)

---

**Step 3 : Ajouter les 5 fonctions pures dans `modules/dialog_register_detail.py`**

Juste après `fmt_reel32` (ligne 50), avant le commentaire `# ─── Dialogue ─`, insérer :

```python
def parse_bin_byte(text: str) -> int:
    """Parse une chaîne binaire 8 bits → int (0-255). Lève ValueError si invalide."""
    val = int(text, 2)
    if not (0 <= val <= 255):
        raise ValueError(f"Hors plage: {val}")
    return val


def parse_octet(text: str) -> int:
    """Parse un texte décimal ou hex (0x..) → int (0-255). Lève ValueError si invalide."""
    val = int(text, 0)
    if not (0 <= val <= 255):
        raise ValueError(f"Hors plage: {val}")
    return val


def parse_mot16(text: str) -> int:
    """Parse décimal ou hex → int (0-65535). Lève ValueError si hors plage."""
    val = int(text, 0)
    if not (0 <= val <= 65535):
        raise ValueError(f"Hors plage: {val}")
    return val


def parse_mot32(text: str) -> int:
    """Parse décimal ou hex → int (0-4294967295). Lève ValueError si hors plage."""
    val = int(text, 0)
    if not (0 <= val <= 0xFFFFFFFF):
        raise ValueError(f"Hors plage: {val}")
    return val


def parse_reel32_to_words(text: str) -> tuple:
    """Parse un float → (word_hi, word_lo) IEEE 754 big-endian. Lève ValueError si invalide."""
    val = float(text)
    raw = struct.pack(">f", val)
    w_hi = (raw[0] << 8) | raw[1]
    w_lo = (raw[2] << 8) | raw[3]
    return w_hi, w_lo
```

---

**Step 4 : Vérifier que les 10 nouveaux tests passent + aucune régression**

```bash
C:\Python314\python.exe -m pytest tests/ -v
```
Attendu : 77 passed (67 + 10)

---

**Step 5 : Commit**

```bash
git add modules/dialog_register_detail.py tests/test_dialog_detail.py
git commit -m "feat: add parse_bin_byte/octet/mot16/mot32/reel32_to_words pure functions with tests"
```

---

## Task 3 : dialog_register_detail.py — Refonte grille (Entry + bits cliquables)

**Files:**
- Modify: `modules/dialog_register_detail.py`

Objectif : réécrire `__init__`, `_build_bits`, `_build_grid`, `_refresh` pour
utiliser Entry à la place des Labels et rendre les bits cliquables.
La ligne ASCII est supprimée.

---

**Step 1 : Mettre à jour `__init__`**

Dans `__init__` (ligne 56), modifier le bloc d'initialisation des variables :

```python
    def __init__(self, parent, address: int, w0: int, w1: int):
        super().__init__(parent)
        self.title(f"Détails — N° Registre : {address}")
        self.resizable(False, False)
        self.transient(parent)

        self._address = address
        self._w0 = w0 & 0xFFFF
        self._w1 = w1 & 0xFFFF

        self.hexa_var        = tk.BooleanVar(value=False)
        self.unsigned_var    = tk.BooleanVar(value=False)
        self.swap_bytes_var  = tk.BooleanVar(value=False)
        self.swap_words_var  = tk.BooleanVar(value=False)

        # Anti-boucle : bloqué pendant _refresh()
        self._updating: bool = False

        # StringVars pour les Entry éditables
        self._bin_vars: list = []    # 4 Entry binaire (Octet 3..0)
        self._oct_vars: list = []    # 4 Entry octet
        self._mot16_vars: list = []  # 2 Entry Mot 16
        self._mot32_var: tk.StringVar = tk.StringVar()
        self._reel32_var: tk.StringVar = tk.StringVar()
        self._bit_vars: list = []    # 32 BooleanVar bits

        self._build()
        self._refresh()

        self.update_idletasks()
        px = parent.winfo_rootx() + parent.winfo_width() // 2
        py = parent.winfo_rooty() + parent.winfo_height() // 2
        w, h = self.winfo_width(), self.winfo_height()
        self.geometry(f"+{px - w // 2}+{py - h // 2}")

        self.grab_set()
        self.wait_window()
```

---

**Step 2 : Réécrire `_build_bits` — checkboxes NORMAL + command**

Remplacer la méthode `_build_bits` entière :

```python
    def _build_bits(self, parent):
        """Deux rangées de 16 checkboxes cliquables représentant les bits."""
        frame = ttk.LabelFrame(parent, text=f"Bits — N° Registre : {self._address}")
        frame.pack(fill=X)

        self._bit_vars = [tk.BooleanVar(value=False) for _ in range(32)]

        for row_idx, (start, end) in enumerate([(15, -1), (31, 15)]):
            num_row = ttk.Frame(frame)
            num_row.pack()
            bit_row = ttk.Frame(frame)
            bit_row.pack()
            for col, bit in enumerate(range(start, end, -1)):
                ttk.Label(num_row, text=str(bit), width=3, anchor=CENTER).grid(
                    row=0, column=col)
                cb = ttk.Checkbutton(
                    bit_row,
                    variable=self._bit_vars[bit],
                    command=lambda b=bit: self._on_bit_click(b),
                )
                cb.grid(row=0, column=col)
```

---

**Step 3 : Réécrire `_build_grid` — Entry widgets, sans ASCII**

Remplacer la méthode `_build_grid` entière :

```python
    def _build_grid(self, parent):
        """Grille : 4 colonnes Octet × 5 types (sans ASCII) + Options."""
        container = ttk.Frame(parent)
        container.pack(fill=X)

        # ── Table ──────────────────────────────────────────────────────────
        table = ttk.Frame(container)
        table.grid(row=0, column=0, sticky=W)

        headers = ["", "Octet 3", "Octet 2", "Octet 1", "Octet 0"]
        for c, h in enumerate(headers):
            ttk.Label(table, text=h, width=10, anchor=CENTER,
                      bootstyle=PRIMARY).grid(row=0, column=c, padx=1, pady=1)

        row_names = ["Binaire", "Octet", "Mot 16", "Mot 32", "Réel 32"]
        for r, name in enumerate(row_names, start=1):
            ttk.Label(table, text=name, width=10, anchor=W,
                      bootstyle=SECONDARY).grid(row=r, column=0, padx=1, pady=1)

        # Binaire row (row 1) — 4 Entry 8 chars
        self._bin_vars = [tk.StringVar() for _ in range(4)]
        for c, sv in enumerate(self._bin_vars):
            ttk.Entry(table, textvariable=sv, width=10,
                      justify=CENTER).grid(row=1, column=c + 1, padx=1, pady=1)
            sv.trace_add("write", lambda *_, i=c: self._on_change(f"bin{i}"))

        # Octet row (row 2) — 4 Entry 3 chars
        self._oct_vars = [tk.StringVar() for _ in range(4)]
        for c, sv in enumerate(self._oct_vars):
            ttk.Entry(table, textvariable=sv, width=10,
                      justify=CENTER).grid(row=2, column=c + 1, padx=1, pady=1)
            sv.trace_add("write", lambda *_, i=c: self._on_change(f"oct{i}"))

        # Mot 16 row (row 3) — 2 Entry colspan 2
        self._mot16_vars = [tk.StringVar(), tk.StringVar()]
        for c, sv in enumerate(self._mot16_vars):
            ttk.Entry(table, textvariable=sv, width=21,
                      justify=CENTER).grid(row=3, column=1 + c * 2, columnspan=2,
                                           padx=1, pady=1)
            sv.trace_add("write", lambda *_, i=c: self._on_change(f"mot16_{i}"))

        # Mot 32 row (row 4) — 1 Entry colspan 4
        self._mot32_var = tk.StringVar()
        ttk.Entry(table, textvariable=self._mot32_var, width=43,
                  justify=CENTER).grid(row=4, column=1, columnspan=4, padx=1, pady=1)
        self._mot32_var.trace_add("write", lambda *_: self._on_change("mot32"))

        # Réel 32 row (row 5) — 1 Entry colspan 4
        self._reel32_var = tk.StringVar()
        ttk.Entry(table, textvariable=self._reel32_var, width=43,
                  justify=CENTER).grid(row=5, column=1, columnspan=4, padx=1, pady=1)
        self._reel32_var.trace_add("write", lambda *_: self._on_change("reel32"))

        # ── Options ────────────────────────────────────────────────────────
        opts = ttk.LabelFrame(container, text="Options", padding=8)
        opts.grid(row=0, column=1, sticky=N + W, padx=(12, 0))

        for text, var in [
            ("Hexa",        self.hexa_var),
            ("Non signé",   self.unsigned_var),
            ("Inv. Octets", self.swap_bytes_var),
            ("Inv. Mots",   self.swap_words_var),
        ]:
            ttk.Checkbutton(opts, text=text, variable=var,
                            command=self._refresh).pack(anchor=W, pady=2)
```

---

**Step 4 : Réécrire `_refresh` — utiliser les nouvelles variables**

Remplacer la méthode `_refresh` entière :

```python
    def _refresh(self):
        """Recalcule toutes les cellules depuis _w0/_w1 et les options courantes."""
        sw      = self.swap_words_var.get()
        sb      = self.swap_bytes_var.get()
        hexa    = self.hexa_var.get()
        unsigned = self.unsigned_var.get()

        b3, b2, b1, b0 = decode_registers(self._w0, self._w1, sw, sb)
        bytes_list = [b3, b2, b1, b0]

        # Bits (toujours _w0 bits 0-15 et _w1 bits 0-15, non affectés par les swaps)
        for i in range(16):
            self._bit_vars[i].set(bool((self._w0 >> i) & 1))
        for i in range(16):
            self._bit_vars[16 + i].set(bool((self._w1 >> i) & 1))

        # Binaire
        for i, sv in enumerate(self._bin_vars):
            sv.set(fmt_bin_byte(bytes_list[i]))

        # Octet
        def _fmt_int(v: int) -> str:
            return f"0x{v:02X}" if hexa else str(v)

        for i, sv in enumerate(self._oct_vars):
            sv.set(_fmt_int(bytes_list[i]))

        # Mot 16 (word_hi/lo selon swap_words)
        word_hi, word_lo = (self._w1, self._w0) if sw else (self._w0, self._w1)

        def _fmt_word16(w: int) -> str:
            if hexa:
                return f"0x{w & 0xFFFF:04X}"
            if not unsigned and (w & 0x8000):
                return str((w & 0xFFFF) - 0x10000)
            return str(w & 0xFFFF)

        self._mot16_vars[0].set(_fmt_word16(word_hi))
        self._mot16_vars[1].set(_fmt_word16(word_lo))

        # Mot 32
        combined = ((word_hi & 0xFFFF) << 16) | (word_lo & 0xFFFF)
        if hexa:
            self._mot32_var.set(f"0x{combined:08X}")
        elif not unsigned and (combined & 0x80000000):
            self._mot32_var.set(str(combined - 0x100000000))
        else:
            self._mot32_var.set(str(combined))

        # Réel 32
        self._reel32_var.set(fmt_reel32(word_hi, word_lo))
```

---

**Step 5 : Vérifier l'import et les tests**

```bash
C:\Python314\python.exe -c "from modules.dialog_register_detail import RegisterDetailDialog; print('OK')"
C:\Python314\python.exe -m pytest tests/ -v
```
Attendu : Import OK, 77 passed

---

**Step 6 : Commit**

```bash
git add modules/dialog_register_detail.py
git commit -m "feat: refactor dialog to Entry widgets, remove ASCII row, make bits clickable"
```

---

## Task 4 : dialog_register_detail.py — Synchronisation dynamique anti-boucle

**Files:**
- Modify: `modules/dialog_register_detail.py`

Ajouter les méthodes `_inject_byte`, `_on_bit_click`, `_on_change`.

---

**Step 1 : Ajouter `_inject_byte` (helper)**

Ajouter après `_refresh` :

```python
    def _inject_byte(self, byte_index: int, byte_val: int) -> None:
        """Injecte un octet (0-255) à la position byte_index dans _w0/_w1.

        byte_index : 0=Octet3 (MSB), 1=Octet2, 2=Octet1, 3=Octet0 (LSB)
        Tient compte de swap_words et swap_bytes pour écrire dans le bon registre.
        """
        sw = self.swap_words_var.get()
        sb = self.swap_bytes_var.get()

        # Défaire swap_bytes pour retrouver la position naturelle dans combined
        if sb:
            byte_index = [1, 0, 3, 2][byte_index]

        # position naturelle → registre et octet
        # combined = (hi << 16) | lo, hi=_w0 ou _w1 selon sw
        hi_attr, lo_attr = ("_w1", "_w0") if sw else ("_w0", "_w1")

        if byte_index == 0:        # hi high byte
            hi = getattr(self, hi_attr)
            setattr(self, hi_attr, (hi & 0x00FF) | (byte_val << 8))
        elif byte_index == 1:      # hi low byte
            hi = getattr(self, hi_attr)
            setattr(self, hi_attr, (hi & 0xFF00) | byte_val)
        elif byte_index == 2:      # lo high byte
            lo = getattr(self, lo_attr)
            setattr(self, lo_attr, (lo & 0x00FF) | (byte_val << 8))
        elif byte_index == 3:      # lo low byte
            lo = getattr(self, lo_attr)
            setattr(self, lo_attr, (lo & 0xFF00) | byte_val)

        self._w0 &= 0xFFFF
        self._w1 &= 0xFFFF
```

---

**Step 2 : Ajouter `_on_bit_click`**

Ajouter après `_inject_byte` :

```python
    def _on_bit_click(self, bit: int) -> None:
        """Callback quand un bit est cliqué. Met à jour _w0 ou _w1 et rafraîchit."""
        if self._updating:
            return
        self._updating = True
        try:
            if bit < 16:
                if self._bit_vars[bit].get():
                    self._w0 |= (1 << bit)
                else:
                    self._w0 &= ~(1 << bit)
            else:
                b = bit - 16
                if self._bit_vars[bit].get():
                    self._w1 |= (1 << b)
                else:
                    self._w1 &= ~(1 << b)
            self._w0 &= 0xFFFF
            self._w1 &= 0xFFFF
            self._refresh()
        finally:
            self._updating = False
```

---

**Step 3 : Ajouter `_on_change`**

Ajouter après `_on_bit_click` :

```python
    def _on_change(self, source: str) -> None:
        """Callback commun pour tous les Entry éditables.

        Principe anti-boucle : _updating=True bloque les callbacks déclenchés
        par _refresh() lors de la mise à jour des autres StringVars.
        """
        if self._updating:
            return
        self._updating = True
        try:
            sw = self.swap_words_var.get()
            hi_attr, lo_attr = ("_w1", "_w0") if sw else ("_w0", "_w1")

            if source.startswith("bin"):
                idx = int(source[3])   # 0=Octet3 .. 3=Octet0
                byte_val = parse_bin_byte(self._bin_vars[idx].get())
                self._inject_byte(idx, byte_val)

            elif source.startswith("oct"):
                idx = int(source[3])
                byte_val = parse_octet(self._oct_vars[idx].get())
                self._inject_byte(idx, byte_val)

            elif source == "mot16_0":
                val = parse_mot16(self._mot16_vars[0].get())
                setattr(self, hi_attr, val)

            elif source == "mot16_1":
                val = parse_mot16(self._mot16_vars[1].get())
                setattr(self, lo_attr, val)

            elif source == "mot32":
                val = parse_mot32(self._mot32_var.get())
                setattr(self, hi_attr, (val >> 16) & 0xFFFF)
                setattr(self, lo_attr, val & 0xFFFF)

            elif source == "reel32":
                w_hi, w_lo = parse_reel32_to_words(self._reel32_var.get())
                setattr(self, hi_attr, w_hi)
                setattr(self, lo_attr, w_lo)

            self._refresh()

        except ValueError:
            pass  # Saisie incomplète (ex: "0x" seul) — ignorer silencieusement

        finally:
            self._updating = False
```

---

**Step 4 : Vérifier l'import et les tests**

```bash
C:\Python314\python.exe -c "from modules.dialog_register_detail import RegisterDetailDialog; print('OK')"
C:\Python314\python.exe -m pytest tests/ -v
```
Attendu : Import OK, 77 passed

---

**Step 5 : Commit**

```bash
git add modules/dialog_register_detail.py
git commit -m "feat: add dynamic sync (_on_change, _on_bit_click, _inject_byte) to dialog"
```

---

## Vérification finale

```bash
C:\Python314\python.exe -m pytest tests/ -v
```
Attendu : **77 passed**

```bash
C:\Python314\python.exe -m PyInstaller build.spec --clean
```
Attendu : `Build complete!` → `dist/LOGICIEL_IP.exe`

```bash
git log --oneline -6
```
